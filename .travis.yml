dist: trusty
sudo: false

language: node_js
node_js:
  - "8"
  
addons:
  chrome: stable

cache:
  directories:
     - ./node_modules

install:
  - npm install

script:
  - npm test
  # - npm run e2e -- --no-progress --config=protractor-ci.conf.js